# .env.example — Copia este archivo a `.env` y rellena los valores reales.
# NO subas `.env` al repositorio ni compartas las credenciales públicamente.

# SMTP settings (Hostinger example)
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=admin@onereserve.es
SMTP_PASS=your_smtp_password_here

# Email addresses
FROM_EMAIL=admin@onereserve.es
TO_EMAIL=admin@onereserve.es

# App port (la plataforma suele inyectar PORT automáticamente)
PORT=3000
